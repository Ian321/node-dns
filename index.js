const parse = require('./dns/parse');
const conversion = require('./dns/conversion');
const typeMap = require('./dns/typeMap');
const via = require('./dns/via');

module.exports = {
  parse, conversion, typeMap, via
};
